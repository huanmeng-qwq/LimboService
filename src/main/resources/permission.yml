groups:
  admin:
    - limboserver.stop
    - limboserver.kick
    - limboserver.say
    - limboserver.gamemode
    - limboserver.version
  default:
    - limboserver.spawn
    - limboserver.chat

players:
  LOOHP: 
    - admin